# 彩票中奖测算工具

基于 Kotlin Multiplatform 和 Compose Multiplatform 的跨平台项目。V1 优先交付 Android 和 iOS，Windows、macOS 保留已完成的工程与 OCR 底座，延期到 V1.1 或后续版本。

## 开发推进进度

更新日期：2026-08-20。当前主开发阶段为 **移动端 B3 OCR 准确率、B4 低置信字段与 B5 当前双模拟器真实闭环收尾**。

| 批次 | 状态 | 已完成 | 未完成或验收差距 |
|---|---|---|---|
| B0 需求与票样基线 | 进行中 | 产品边界、技术方案、支持门槛和隐私规则已固化；已完成 13 张真实图片的本地探索 | 样本数量、省份和拍摄条件不足；未建立双人真值，支持白名单保持为空 |
| B1 跨平台工程骨架 | 核心完成 | 建立 Android、iOS、Windows/macOS 宿主、共享模块、Compose 页面壳、CI 和开发构建链路 | 桌面发布验证延期，不阻塞移动 V1 |
| B2 开奖查询与规则引擎 | 核心完成，待验收 | 已实现体彩/福彩适配器、双源证据状态、两种彩票现行规则、倍数和大乐透追加计算；大乐透历史期可使用同一期官方 PDF 独立核对，Android PDFBox 与 iOS PDFKit 共用严格解析契约；两端系统网络栈均已完成公开历史期双证据验证，并完成 40/40 期机器首轮对账 | 20 期台账第二人复核、连续 6 个开奖窗口采样、数据授权和临时派奖规则仍未完成；iPhone 应用级完整交互仍待复测 |
| B3 移动图片采集与 OCR PoC | 进行中 | Android/iOS 拍照、系统选图、本地 OCR 和保守解析已贯通；分辨率、严重整体曝光、明显模糊、初步裁切、明显透视与明显倾斜闸门已接入；共享解析现把平台原始行置信度保守聚合到彩种、期号、投注行、倍数、追加、期数和金额区域，并完成当前 13 图的分平台匿名初步分布 | 真实裁切/透视、阴影与局部反光准确率，自动旋转和透视校正、OCR 双人真值、平台分别校准和批量准确率统计未完成 |
| B4 移动票面解析与人工校正 | 进行中 | 已实现彩种/期号校正、号码球、倍数与期数步进、追加选择、金额推导、候选选择及 `ConfirmedTicket` 来源追踪；当前 OCR 引擎名和字段原始置信度只在校正流程内存中保留，Debug 包可输出不含票面内容的匿名分布样本；彩种与期数也可携带证据区域 | Android ML Kit 与 iOS Vision 尚未结合逐 token 真值分别校准，低置信视觉标记和足量真实票样验收未完成；复式、胆拖、补打及未知序列仍阻断 |
| B5 移动核心端到端闭环 | 核心完成，待验收 | Android/iOS 已接入拍照/OCR、原图辅助录入与无图手动录入、精确期号官网查询、本地规则计算、逐注结果、双源证据及重试；当前 Android 36.1 与 iOS 26.5 模拟器已完成五注十期真实票图、逐期双证据、首个未发布期停止、断网保留既有证据及主动重查闭环 | Android/iPhone 完整物理真机矩阵、跨年度期次日历、发布窗口、授权及足量票样仍未完成；这些发布差距不在当前仅使用虚拟机的执行范围内冒充完成 |
| B6 Android/iOS V1 发布 | 未开始 | 已建立移动端发布指标并具备 Android/iOS 签名和真机开发链路 | 盲测、性能、AAB/IPA、隐私材料、商店合规和数据授权未完成 |
| V1.1+ Windows/macOS | 延期 | 已完成桌面宿主、导图、无遥测 ONNX 工作进程、PP-OCRv5 来源锁和可复现转换 | 真实桌面 OCR、ZXing、Windows 实机、目标平台裁剪和正式分发均不属于移动 V1 闸门 |

上一轮包含 JVM、Android Host 与 iOS Simulator 的完整自动化基线共 665 项：JVM 227 项、Android Host 220 项、iOS Simulator 218 项，失败、错误和跳过均为 0；本轮按执行约束不再使用 JVM 或 Android Host 作为验收目标，新增回归只在当前 iOS Simulator 和 Android 虚拟机复跑。另有 Android 16 物理机 63 项历史设备测试通过，但不属于本轮验收。Android 和 iOS 核心相机流程已通过真机冒烟；满足当前保守解析条件的真实图片，以及结构安全但缺失期号、倍数、追加或金额及属性矛盾的 OCR 结果可以进入人工校正，并对照原图定位已有字段。多个期号或金额候选只作为原图辅助录入的显式选项，不携带可能来自另一张彩票的其他自动草稿。倍数和追加未知时不会填入默认值，必须由用户明确选择；仅在图片预览可用且领域校验通过后生成 `ConfirmedTicket`。

2026-08-20 修复大乐透最新已发布期在开奖次日 09:00 前首次查询仍误报“发布核对中”的问题。原实现只按固定时钟区分发布窗口，即使历史 JSON、最新聚合 JSON 和同期开奖 PDF 都已发布且一致，仍强制等待 60 秒；现在移动端会严格解析目标期号绑定的官方 PDF，并在期号、日期、号码、全部奖级和奖金乘法与两个 JSON 数据面三方一致时直接形成终态。PDF 缺失、结构漂移、字段不完整或任一证据冲突时仍保留原等待或失败关闭规则。Android/iOS 两端固定在 09:00 前的正、负回归均通过；随后从干净状态安装同一提交并导入指定五注十期原图，两端都形成起始 `26090`、1 倍、10 期和 100.00 元完整草稿，结果页完成 5/10 期，`26094` 为奖金终态、`26095` 尚未发布、`26096` 未发起查询，私有临时票图均已清空。Android 正式设备测试链路直接通过目标期号；iOS 安装应用的 Darwin 链路同样通过，但隔离的 Kotlin/Native 测试进程连续两次返回网络不可用，保留为测试运行环境差异且没有加入 TLS 绕过。最终当前 iOS Simulator 回归 225/225、Android 虚拟机数据层设备回归 66/66。当前 Android APK 为 64,855,376 字节，SHA-256 为 `1630c15bafb702a0093c024e0875e674a69cc6c7e904989ac143e87b5dd9b51d`，iOS Simulator 应用由 Xcode 完整构建成功。

同日补齐多期主动重查的两条安全契约。已完成票据遇到官网数据源整体异常时，只请求首个目标期并停止后续请求，保留全部既有开奖号码和测算结果，同时显示本次重查未完成；恢复后再完整刷新。若新响应明确给出官方证据冲突，则不保留受影响期的旧结论，只降级该期、保留其他期次并撤销整票结论；后续恢复时仅补查冲突期。两条路径都不会因失败重查输出新的中奖或未中奖结论。

同日只在当前双模拟器完成多期官网数据源异常应用级验收。两端均从空白手动录入构造起始 `26072` 的 3 期大乐透合成票，金额关系完整；首期取得最终号码证据，`26073` 的当前历史奖级数据触发既有严格规则闸门并明确显示“官方数据源不可用”，`26074` 因前一期整体数据源异常未发起查询，整票保持 1/3 且结论未形成。两端主动重查后首期强证据继续保留，异常期和后续停止状态不变。验收没有记录合成投注号码，也没有操作已连接的实体或非目标 ADB 设备。

随后完成多期官方证据冲突的双端应用级验收。一次性装饰器只允许 Debug 包通过合法目标期号启用，每次仍先执行真实官网 HTTPS 查询，不改响应、不伪造证据且不降低 TLS 校验；目标期第二次成功查询仅被转换为一次 `CONFLICT`，后续查询恢复真实结果。Android 36.1 与 iOS 26.5 模拟器从 `2026093` 起的 3 期双色球手动票初查均为 3/3 且每期两份官方证据；首次重查后只有 `2026094` 降为“官方证据冲突”，其余两期证据保留、整票结论撤销，再次重查恢复 3/3。两端返回首页后临时票图目录为空，固定脚本完成 iOS 228/228、Android 66/66 回归且 101 个任务全部实际执行。

同一增量随后完成非 Debug 包的反向验收。Android `assembleRelease` 产物经临时测试签名后只安装到当前模拟器，包清单和运行时标记均不含 `DEBUGGABLE`；iOS 使用 Xcode `Release` 配置的 Simulator 应用。两端都传入与 Debug 验收相同的 `2026094` 冲突参数，并从空白手动录入相同结构的 3 期双色球合成票；初查与首次主动重查均保持 3/3、每期两份官方证据，未出现“官方证据冲突”，按钮始终为“重新查询全部期次”。这证明一次性装饰器无法由非 Debug 包的外部参数启用。iOS 临时票图目录为空；Android Release 正确拒绝 Shell 读取私有缓存，本轮手动路径未调用图片采集。Appium 会话与服务均已关闭。

同日为移动像素质量闸门增加保守的明显梯形透视检测。算法只比较左右半区近竖直强边缘的局部主轴：两侧边缘比例均不少于 3%、集中度均不少于 0.28 且按 90 度等价后的夹角超过 12 度时才拒绝；轻微合成梯形继续放行。iOS Simulator 以生产阈值匿名复跑根目录 13 张探索图，13/13 全部通过；Android 指定原图继续进入票面流程，安全边距内的明显梯形临时变体只命中透视提示，没有误报裁切或旋转。该结果只证明确定性边界和当前探索样本无新增误拒，真实拍摄角度的误拒率、漏检率和自动透视校正仍未完成。

同日自主找到票面可证明为 2025 年的大乐透和 2026 年的双色球公开图，只在当前 Android 36.1 与 iOS 26.5 模拟器验证生产图片链路。大乐透公开图在 Android 形成安全草稿、iOS 进入辅助校正，但因票面期号早于大乐透 V1 `26014` 规则边界没有进入开奖计算；双色球公开图在 Android 失败关闭，iOS 经原图校正后取得奖金终态和两份福彩官方证据。公开图经过重新编码和放大，不能代表原始拍摄质量，也不计入当前 13 张样本、白名单、准确率或盲测；完整匿名记录见[近期公开票样探索记录](docs/research/public-ticket-style-exploration-2026-08-20.md)。

随后继续找到江苏体彩公开的 2026 年大乐透单期追加票。当前 Android 与 iOS 模拟器均因号码行不完整安全进入全字段空白辅助录入；按原图校正为五注、1 倍、单期追加和 15.00 元后，两端都得到前四注七等奖、第五注追加一等奖、税前总奖金 12,213,634.00 元及两份中国体彩网官方证据。官方原图仅 533×720 像素且带中奖行标注，本轮放大副本只证明失败关闭、人工校正和查询闭环，不代表原始拍摄质量，也不计入样本、白名单、准确率或盲测。

2026-08-17 按当前执行约束只使用现有 Android 36.1 与 iOS 26.5 模拟器完成多候选专项验收，没有操作实体设备。两端导入同一张不含真实票据数据的临时合成票面后，均由本地 OCR 识别出两个合法期号和两个合法金额，保留原图及候选并进入全字段空白录入，没有自动混入号码或投注属性。Android 完成期号与金额点选，iOS 完成金额点选并确认选中态；其余未确认字段继续阻断查询，页面滚动、候选横向布局和错误提示均无裁切或重叠。临时票面与截图不进入 Git，也不计入真实票样准确率。

同日完成 OCR 原始置信度的共享透传基础：字段区域保存平台校准前的原始值，同一视觉行或跨行字段只在全部必要片段均提供合法分数时取最低值，任一片段缺失或越界则保持未知，不会忽略缺失值后抬高置信度。校正状态同时保留当前 OCR 引擎名；手动录入、返回后的无图快照和流程退出均不保留这些元数据。当前没有设置 Android/iOS 通用阈值，原始分数不参与确认、猜号或查询决策。两端当前模拟器使用五注十期探索图复验后仍形成一致安全草稿，退出后的票图目录为空。

2026-08-18 补齐彩种和投注期数的字段证据。精确彩种标题优先使用真正包含关键词且面积最小的原始 OCR 框；标题缺失时，只有发行机构与唯一合法单式号码结构一致才形成联合证据，并保守取最低原始置信度。期数只在票面明确给出或金额关系只能恢复出唯一值时生成区域，默认单期不会伪造期数证据，冲突期数继续阻断。最终包只在当前 `Medium_Phone_API_36.1` 与 iPhone 17 Pro / iOS 26.5 模拟器复验：两端仍得到五注、1 倍基本投注、10 期和 100.00 元安全草稿；过高的彩种框均未显示为定位入口，“期数”准确高亮 `10期1倍` 摘要，横向控件无重叠。返回首页后 Android 目录仅剩 `.nomedia`，iOS 临时票图目录为空；本轮未操作实体设备。平台分别校准和低置信视觉标记仍未完成。

同日继续只在当前双模拟器复验一张单期双色球探索图。Android ML Kit 形成期号、五注、1 倍、单期和 10.00 元完整安全草稿；覆盖过高的彩种区域继续隐藏。iOS Vision 因至少一行号码不完整安全转入全字段空白辅助录入，只保留紧凑彩种、期号和金额原图区域，没有携带部分号码；彩种入口准确定位到票面玩法标题。两端都没有为默认单期生成“期数”定位入口，返回首页后临时票图清理完成。本结果只固定失败关闭和证据保留契约，不构成准确率、版式白名单或置信度阈值结论。

同日使用现有五注十期大乐透探索图完成多期查询返回专项。Android/iOS 均在首期官网查询发出后立即返回，恢复不再引用已删除原图的确认票据；等待迟到结果后页面仍保持，没有跳转到结果或错误页。共享控制器新增回归，进一步断言首期迟到结果被丢弃且第二期及之后从未发起请求。两端随后返回首页，临时票图目录继续为空。本轮未使用实体设备，也没有把模拟器相册中的多期、补打或来源不明图片冒充当前仍缺失的大乐透 V1 单期票样。

2026-08-19 修复双色球普通状态证据边界停在 `2026093` 导致已发布期误报“发布核对中”的问题。逐期核验确认 `2026094`、`2026095` 的主、详情 JSON 在期号、日期、号码、六个基础奖级和详情链接上完全一致，福运奖、特别规定及派奖字段均为空；代码只将边界滚动至 `2026095`，`2026096` 继续保守阻断。Android 36.1 模拟器使用用户指定原图形成五注草稿，人工改为 10 期和 100.00 元后显示已完成 4/10 期，`2026094/95` 均为奖金终态和两份官方证据，`2026096` 才显示尚未发布；iOS 26.5 Simulator 的 Darwin 双期 smoke 同样通过。双色球独立滚动 20 期双源对账为 20/20；合并 40 期复跑被大乐透 `26073` 当前上游追加金额触发既有 80% 闸门，已单独保留为待核问题，不影响本次双色球修复结论。

同日修复多期主动重查遇到瞬时网络故障时覆盖既有强证据的问题。修复前，起始 `26090` 的 10 期大乐透手动票已完成 4/10 期，其中 `26091` 已确认奖级但奖金待补；断网重查会降为 3/10 并丢失该期开奖号码和奖级。修复后，Android 36.1 与 iOS 26.5 模拟器均保持 4/10、`26091` 奖级和两份证据，并明确显示本次重查未完成且旧结果已保留；恢复联网后提示清除，新证据正常刷新。两端新增提示在 1080×2400 像素和 402×874 点界面均无裁切或重叠；本轮没有操作实体设备，退出后临时票图目录为空。

同日为 Debug 包新增结构化匿名 OCR 置信度诊断，类型边界只允许解析状态、字段分类和原始分数，不接收图片标识、路径、OCR 文本、坐标、号码或金额；Android Release 非可调试包与 iOS Release 二进制完全禁用。当前 13 张探索图在 Android ML Kit 上得到 `ready=2 / correction=10 / unsupported=1`，在 iOS Vision 上得到 `ready=3 / correction=8 / unsupported=2`；分别有 5 张和 4 张未形成任何安全字段。Vision 已形成字段的分数只出现 `0.3/0.5/1.0`，ML Kit 则为连续分布，证明不能设置跨平台统一阈值。当前没有逐 token 双人真值，因此该结果只是分布基线，不是准确率或低置信阈值结论。

同日继续只使用当前 `Medium_Phone_API_36.1` 与 iPhone 17 Pro / iOS 26.5 模拟器，复跑五注十期大乐透探索票的完整图片闭环。两端本地 OCR 均形成相同的起始期号、五注、1 倍基本投注、10 期和 100.00 元安全草稿；确认后，前三个已发布期分别取得两份官网证据和独立测算结果，首个未发布期明确停止本轮后续请求，其余期次均标记为尚未发起。两端主动重查后保持 3/10 期完成和整票结论未形成，没有把部分未完成票据解释为整票未中奖；返回首页后 Android 私有票图目录只剩 `.nomedia`，iOS 临时票图目录为空。文档不记录实体票号码、逐注结果或票图，验收页面源与截图只保存在本机临时目录。

2026-08-15 在 Android 16 和 iOS 26.5 模拟器完成移动图片质量闸门验收：同一张清晰探索图在两端均继续进入本地 OCR，没有被像素规则误拒；确定性生成的明显模糊、严重过暗、严重过亮和 20 度明显倾斜变体分别只命中对应提示，没有交叉误报。两端异常流程结束后，Android 私有目录只保留 `.nomedia`，iOS 私有临时票图目录为空；倾斜提示在 402 点宽 iOS 界面及 Android 错误页均无裁切或重叠。阈值只依据当前 13 张探索图的匿名指标和确定性变体保守校准，不等同于真实多条件拍摄真值或版式白名单。

同日针对一张此前两端均无法形成草稿的五注多期大乐透探索图完成专项修复。早期失败来自标题及分区符误识别、侧边纵排发行机构文字跨行干扰；最后一个 Android 阻点是 ML Kit 把期数、倍数及单位吞并成连续数字。共享解析器现只在“单式票或追加投注 + 短摘要 + 合计金额”锚点完整、彩种与注数明确、票面金额只能对应一组期数和倍数时恢复字段；存在多个同额解释或明确冲突证据时仍保持未知。Android 16 与 iOS 26.5 模拟器使用同一原始 JPEG 复验后，均进入“确认票面信息”，自动形成五注、1 倍基本投注、10 期且票面与理论金额同为 100 元的草稿。Android 当时执行真实逐期查询，已发布历史期因尚无独立公告解析而保持“发布核对中”，遇到首个尚未发布期后不再请求更晚期次，也没有误报整票未中奖；该证据缺口已在 2026-08-17 的下一阶段补齐。领域层现允许受控的 1 至 20 期连续单式票按全部期数严格核对金额并逐期查询；这次单图通过及代码回归不等同于完成物理真机交互或达到公开发布准确率。

2026-08-17 将大乐透官方 PDF 公告解析移入移动运行时：只接受与期号完全一致的固定 HTTPS 地址、最多 1 MB、PDF 1.4、单页、未加密、无表单和固定 iText 生产器，随后核对日期、七个号码、七个基础奖级、一二等奖追加行及每行“注数 × 单注奖金 = 合计”。Android 16 物理机使用公开 `26090` 期完成主 JSON、聚合回退、PDFBox 文本层、双证据一致和 `FINAL_NUMBERS` 真实闭环。iOS 26.5 Simulator 早期曾以 `NSURLError -1202` 拒绝体彩证书链；同日后续在明确移除验收代理后，项目 Darwin 客户端已完成同一期主 JSON、聚合回退、官方 PDF 下载、PDFKit 解析和双证据闭环，期间没有添加证书忽略或降低 TLS 校验的旁路。当前 iPhone Debug 包也已重新签名、覆盖安装、启动并保持进程存活，但应用级完整点击流程仍待物理机复测。

同日修复系统返回直接退出应用的问题。共享 UI 只在非首页注册跨平台 BackHandler：分析、确认、查询、结果、错误和关于页都会回到有效上一级；首次查询结果返回票面确认页，重查过程中返回恢复重查前结果，进行中的查询会失效且迟到响应不能覆盖页面。顶部返回箭头执行上一级导航，明确写着“返回首页”的按钮仍清理流程。Android API 36.1 已分别验证实体返回键和左边缘手势从关于页回到首页且 Activity 保持前台，首页再次返回才由系统退出。

Android/iOS 首页已新增“手动录入彩票”入口，用于绕过 OCR 波动验证单期多注单式票。该流程不采集、不创建也不删除临时图片，从未选彩种、空期号、一行空号码、未确认倍数/追加和空金额开始；选择彩种前不展示号码球，用户可按实体票新增或删除投注行，任一关键字段未确认时都不会发起查询。完整录入后仍复用相同的领域校验、整票金额校验、精确期号官网查询、双源证据和本地规则引擎，不存在降低安全标准的旁路。

2026-08-14 已在 Android 16 和 iOS 26.5 模拟器使用同一张实体双色球票完成真实结果交互验收：精确查询 `2026092` 期，5 注、1 倍、合计 10.00 元，两端均取得奖金终态和两份中国福彩网证据，逐注结果一致为未中奖；正常重查保持同一期同一结论。Android/iOS 均已验证断网重查只显示网络不可用，不形成中奖或未中奖结论，恢复联网后可在同一应用进程使用保留票据重新取得相同真实结果。含实体票号码的验收截图只保存在本机临时目录，不纳入仓库。

2026-08-15 使用真实票图复验从图片到结果的完整路径：Android 本地 OCR 形成的彩种、期号和 5 注安全草稿经逐项对照后直接确认；iOS Vision 因草稿不完整进入“对照原图录入”，所有关键字段保持空白，由用户逐项录入并通过相同领域闸门。两端最终查询同一期真实开奖，逐注结论、奖金终态和两份官网证据一致。iOS 期号及金额输入已提供键盘完成动作和可见完成按钮；确认或返回首页后，Android 私有目录只保留 `.nomedia`，iOS 临时票图目录为空。一次观察到的 iOS 图片文件经核查仍由未退出的校正流程持有，点击返回后立即删除，并非首页残留。

同日又使用双色球 `2026092` 期公开官方开奖号码 `09 11 12 25 30 33 + 11` 构造 1 注、1 倍、2.00 元手动票据，完成 Android/iOS 真实中奖结果验收。两端均展示一等奖、逐注命中 `6 + 1`、税前奖金 `6,466,298.00 元`、奖金终态和两份中国福彩网证据；主动重查后结论与金额不变，证据获取时间正常更新。两端还使用未来期号 `2026999` 和结构完整票据验证安全阻断，均显示“尚未发布”且明确未形成中奖或未中奖结论，没有误报。相关截图只保存在本机临时目录，不纳入仓库。

在 B2 的 20 + 20 期机器对账代码合入后，又从干净应用状态重新构建并安装当前 Android 16 与 iOS 26.5 版本，复跑上述公开一等奖票据。两端首次查询和主动重查仍得到相同一等奖、`6 + 1`、`6,466,298.00 元`及两份中国福彩网证据，重查后的证据获取时间均发生更新；返回首页再进入手动录入后，彩种、期号和号码均为空。随后规定的 471 项 JVM、Android Host 与 iOS Simulator 离线回归再次全部通过，说明本轮 B2 增量没有破坏 B5 移动闭环；这仍不能替代 Android/iPhone 物理真机和正式发布验收。

两端又分别在 Android `font_scale=2.0` 和 iOS 最大辅助字号下复测未来期号安全阻断、主动重查、返回首页及新流程空白状态；标题、说明和操作按钮均可滚动访问，没有状态残留或真实裁切。iOS Debug 另提供只读取 `WINLOTTERY_IOS_ACCEPTANCE_PROXY_PORT` 的本机验收入口，主机固定为 `127.0.0.1` 且端口严格校验；Release 二进制完全忽略该变量。

Android/iOS 确认票面后，单期票只查询用户确认的精确期号；受控多期票按期号顺序分别执行同一套双官网证据核对和本地规则计算。发布核对中或单期冲突只影响对应期次，首个尚未发布期、网络不可用或数据源整体不可用会停止本轮后续请求并保留逐期状态。结果页展示开奖号码、逐注命中数、奖级、税前金额或“奖金待官方确认”、来源与证据摘要；任一期未完成时都不会形成整票“未中奖”，只有全部期次结论和金额完整时才安全汇总税前奖金。确认或退出后会清理应用私有临时图片。2026-08-15 已对大乐透 `26091` 和双色球 `2026093` 完成低频真实官网冒烟，两期规范化结果均成功驱动本地规则引擎识别合成一等奖，测试未打印或保存官网原始响应。

2026-08-14 完成 B2 历史真实开奖机器首轮对账，2026-08-15 将双色球台账滚动至最新已发布期：大乐透固定台账为 `26014` 加 `26073` 至 `26091`，逐期比较中国体彩网历史 JSON 与独立官方 PDF；双色球固定台账为 `2026014` 加 `2026075` 至 `2026093`，逐期复用移动端 Repository 比较中国福彩网主、详情 JSON。两种彩票各 20 期、合计 40/40 期的期号、日期、号码和必要奖级一致，且每期均能驱动规则引擎识别合成一等奖；台账同时硬性覆盖规则首期、最新期、浮动奖、无人中出奖级、大乐透追加奖及双色球特别规定退出边界。这只是机器首轮，不等同于第二人独立真值复核或 B2 发布验收通过。

同日使用中国体彩网 `26091` 期公开开奖结果 `03 04 07 12 32 + 01 02`，在当前 Android 16 与 iOS 26.5 模拟器完成一次不中断的大乐透双端真实交互验收。1 注、1 倍、2.00 元基本投注在两端均显示一等奖和前区命中 5 个、后区命中 2 个；将后区改为 `01 03` 的 1 注、1 倍、3.00 元追加投注均显示二等奖和 `5 + 1`。该期官方数据缺少追加一等奖金额，因此仓库按 `FINAL_NUMBERS` 保守返回，两条路径均只显示“奖金待官方确认”，没有越权展示确定金额；两份中国体彩网证据、基本投注主动重查后的获取时间更新，以及两条流程返回首页后的空白录入状态均已验证。截图和页面源只保存在本机临时目录，不纳入仓库。

同日重新核对国家体育总局体育彩票管理中心的 2026 年变更公告和正式规则 PDF。新七奖级规则自 `26014` 期起执行，公告发布前已售多期票中从该期开始的后续奖期同样执行新规则；现有 13 种命中组合全部一致。实现层要求大乐透结果恰好包含七个基础奖级，三至七等奖必须整体属于奖池低于 8 亿元或达到 8 亿元（含）的同一套固定奖档，一、二等奖已发布追加奖金必须符合基本奖金 80% 的元级公布口径；缺失、重复、未知、混档或比例异常均不能进入确定奖金状态。浮动奖继续只读取当期官网值，临时派奖继续保守阻断。

同日将双色球最新已发布期滚动到 `2026093` 后，使用该期公开开奖号码在 Android 16 与 iOS 26.5 模拟器完成双端真实交互验收。1 注、1 倍、2.00 元基本投注在两端均显示一等奖、红球命中 6 个、蓝球命中 1 个、税前奖金 `6,581,443.00 元`、奖金终态和两份中国福彩网证据；主动重查后两份证据获取时间均更新，奖级和金额保持一致。两端返回首页成功，Android 私有票图目录无临时图片，iOS 临时票图目录为空；截图和页面源只保存在本机临时目录，不纳入仓库。

> 当前开发验证版已具备真实开奖结果查询和中奖测算能力，但不能替代彩票验真或实体票兑奖，也不能解释为支持任何省份、销售终端或票面版式。B0、B2、移动 B3 均未通过发布验收闸门，禁止据此宣称达到公开发布标准。

桌面端已完成 ONNX Runtime 底座、独立本地工作进程，以及 PP-OCRv5 三段官方 Paddle 模型的来源锁定、opset 17 转换和 Paddle/ONNX 合成张量逐元素对比。转换后的约 22.3 MB 模型二进制尚未纳入仓库或安装包，图片预处理、后处理和工作进程推理协议也未实现；这些成果作为 V1.1 或后续版本基础保留，不再阻塞 Android/iOS V1。

开发基线文档：

- [产品与技术方案](docs/01-product-and-architecture.md)
- [开奖数据与中奖测算规则](docs/02-draw-data-and-rules.md)
- [分批开发与验收计划](docs/03-delivery-plan.md)
- [架构决策记录](docs/04-decisions.md)
- [B1 四端工程骨架交付记录](docs/delivery/B1-delivery.md)
- [B2 开奖查询与规则引擎交付记录](docs/delivery/B2-delivery.md)
- [B3 图片采集与 OCR PoC 进展记录](docs/delivery/B3-progress.md)
- [B4 移动端人工校正进展记录](docs/delivery/B4-progress.md)
- [B5 移动端真实开奖结果查询进展记录](docs/delivery/B5-progress.md)
- [首批真实票样探索记录](docs/research/real-ticket-sample-exploration-2026-08-13.md)
- [近期公开票样探索记录](docs/research/public-ticket-style-exploration-2026-08-20.md)

项目首版定位是“纸质彩票中奖测算”，不提供彩票真伪、撤单状态或是否已兑奖的官方核验。

## 模块

```text
:androidApp / :desktopApp / :iosApp
                 ↓
             :composeApp
              ↙       ↘
  :shared:recognition  :shared:data
              ↘       ↙
           :shared:domain
```

## 本地运行

- Android 调试包：`./gradlew :androidApp:assembleDebug`
- macOS 桌面应用：`./gradlew :desktopApp:run`
- 桌面分发 ONNX 验收：`./gradlew :desktopApp:verifyPackagedOnnxRuntime`
- Desktop OCR 模型锁校验：`python3 tools/desktop-ocr/convert_ppocrv5.py --verify-lock-only`
- Desktop OCR 模型转换：`tools/desktop-ocr/convert_ppocrv5.sh`
- JVM 共享测试：`./gradlew jvmTest`
- B2 真实历史对账：`WINLOTTERY_LIVE_RECONCILIATION=1 ./gradlew :shared:data:jvmTest --tests roc.win.lottery.data.OfficialDrawHistoricalReconciliationLiveTest --rerun-tasks`
- 完整共享测试矩阵：`./gradlew jvmTest testAndroidHostTest iosSimulatorArm64Test`
- 当前双虚拟机安全回归：先启动指定 Android AVD 和 iOS Simulator，再执行 `tools/mobile/run-current-vm-tests.sh`；脚本会校验精确设备身份并拒绝回退到其他 ADB 目标。
- 格式检查：`./gradlew spotlessCheck`
- iOS Simulator：用 Xcode 打开 `iosApp/iosApp.xcodeproj`，构建 `iosApp` Scheme
- iOS 真机：参考 `iosApp/Configuration/Local.xcconfig.example` 创建不提交 Git 的 `Local.xcconfig`，填写自己的 `DEVELOPMENT_TEAM_ID` 后构建 `iosApp` Scheme

B0 票样白名单尚未完成。当前已收到 13 张探索图片，并据此实现了共享保守票面解析器、Android/iOS 本地 OCR PoC 及 Windows/macOS 图片导入 PoC。后续只检索和验收能够证明为 2025 年及以后出票的样本；公开网络票图不计入白名单或准确率。现有样本数量、彩种分布、票型和省份覆盖均未达到白名单门槛，因此不承诺自动票面解析范围。
