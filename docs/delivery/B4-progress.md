# B4 Android/iOS 票面人工校正进展记录

记录日期：2026-08-15

## 状态结论

B4 已从静态核对页推进到可用的共享人工校正流程：Android/iOS 真实本地 OCR 草稿可以编辑彩种、期号、逐注号码、倍数、大乐透追加和票面金额，并在每次修改后执行完整领域校验。彩种和投注行结构可靠时，期号、倍数、追加或金额缺失及属性矛盾会保留已识别字段并直接进入校正页；OCR 无法安全形成草稿时不再要求用户放弃当前图片，而是保留私有原图并进入全字段空白的“对照原图录入”，允许按票面增删投注行。未知倍数和追加保持空值，只有用户明确选择、图片预览可用且票面状态合法时才能生成 `ConfirmedTicket` 并继续。

B4 仍处于进行中。OCR 低置信字段标记、期号与金额多候选处理、移动真机预览回归和足量真实票样验收尚未完成，当前不能据此承诺任何省份、终端或票面版式。

## 本轮已实现

### 校正状态与领域对象

- 新增 Android/iOS 共用的不可变 `TicketReviewState` 和单一 `TicketReviewAction`，所有输入通过控制器更新，不在 Composable 内保存第二份业务状态。
- OCR 初始字段标记为 `OCR`；用户实际修改的字段标记为 `USER`；按当前投注结构更新金额时标记为 `DERIVED`。
- 保留投注行原始顺序与 OCR 原文，号码在生成 `ConfirmedTicket` 时按升序保存，重复行不会去重。
- V1 投注期数固定为 1；倍数只接受 1 至 99；未知倍数和大乐透追加保持 `null` 并阻断确认，用户选择后记录为 `USER`；双色球会按规则清除不适用的追加属性。
- 抽取统一 `TicketAmountCalculator`，保守解析器、人工校正和 `TicketValidator` 使用同一套整数分金额公式，不使用浮点数。

### Android/iOS 共用界面

- 彩种使用互斥选择控件，期号和票面金额使用可校验输入框。
- 每注分别提供前区/红球和后区/蓝球号码球编辑器，选中数量达到单式上限后需先取消旧号码再选择新号码。
- 已识别倍数使用带加减图标的步进器，未知倍数提供明确的“确认 1 倍”入口；大乐透基本/追加使用无默认选中的互斥选项，单期约束以只读状态展示。
- 同时展示 OCR 票面金额和按当前投注结构计算的理论金额；用户可明确选择“使用计算金额”，不会静默覆盖 OCR 值。
- 字段问题和汇总问题在当前页面内联显示；确认按钮只有在 `TicketValidator` 完全通过时启用。
- 期号和金额输入保留键盘 `Done` 动作，并增加可见的勾选完成按钮；iOS 数字键盘无法自行收起时仍可明确结束输入。

### 原图预览与字段定位

- 共享保守解析器保留合并后 OCR 视觉行的归一化坐标，并分别关联期号、每一注号码、倍数、追加摘要和票面金额；可恢复草稿同样携带已识别字段区域。
- 校正页提供固定高度的原图预览和“整票/期号/逐注/倍数/追加/金额”切换控件；选择字段后使用带上下文的裁切视口，并在原图中叠加高亮区域。
- 预览位图最长边限制为 1600 像素。Android 使用 `BitmapFactory` 采样并在必要时精确缩小，iOS 使用 Okio 读取私有 JPEG 并由 Skia 按二次幂缩略解码，避免校正页长期持有完整 OCR 尺寸位图。
- 真实识别流程在预览加载期间或加载失败后不会启用确认；加载失败时要求用户返回并重新导入，防止在失去原图核对依据时继续。
- Windows/macOS 解码实现保持空结果，桌面真实 OCR 和校正预览继续按 ADR-011 延期，不扩大移动 V1 范围。

### 流程与安全边界

- 移除真实 OCR 草稿的主动结束阻断，真实票面现在可以进入人工校正并生成不可变确认票据。
- `TicketParseResult.NeedsCorrection` 可携带安全草稿；控制器收到草稿时进入校正页并保留已识别字段，收到无草稿结果时保留临时图片并进入空白手动录入，不把不可靠 OCR 值带入编辑状态。
- 目前可恢复期号、倍数、追加或金额缺失，以及金额、倍数或追加属性矛盾；未知倍数和追加不会参与金额推导。多个期号或金额候选、未知彩种、号码结构异常、复式、胆拖及多期不会选取任一可疑候选，只有用户从空白状态逐项完成合法单期单式票据后才能继续。
- 校验失败保持当前编辑状态和临时图片，便于继续修正；确认成功或退出流程后立即清理应用私有临时图片。
- 本记录形成后，Android/iOS 容器已在 B5 接入真实官网仓库和本地规则引擎；B4 仍只对票面人工校正能力负责，不因此视为通过真实票样验收。
- Windows/macOS 共用代码仍可编译，但桌面客户端已延期到 V1.1 或后续，不属于本批移动 V1 验收范围。

## 自动化覆盖

- 覆盖未修改字段保持 OCR 来源、用户修改期号和号码后记录 USER 来源、倍数变化触发金额不一致、显式金额推导恢复合法状态、空金额阻断。
- 覆盖大乐透草稿切换为双色球后，对期号、号码、追加和金额完成校正并恢复为合法票据。
- 覆盖真实 OCR 容器进入校正页并确认、非法期号停留在确认闸门、不查询开奖且不提前清理票图。
- 覆盖期号、倍数、追加或金额缺失及属性矛盾携带草稿；覆盖多个期号、多个金额、未知彩种和号码结构异常不携带草稿。
- 覆盖控制器对可恢复结果保留图片并进入校正页，以及无安全草稿时保留原图、创建空白投注行、允许新增投注行并在退出后清理图片。
- 覆盖缺失期号必须由用户补齐并记录 `USER` 来源，缺失金额必须由用户输入或明确采用理论金额。
- 覆盖未知倍数和追加保持空值、金额停止推导、控制器分别维持确认闸门，以及用户逐项明确选择后记录 `USER` 来源并恢复合法状态。
- 覆盖大乐透追加、多注、倍数金额计算，双色球基本金额，以及非法追加和追加行数拒绝。
- 覆盖解析结果对期号、逐注号码、倍数、追加和金额区域的映射，以及控制器对可恢复草稿区域的完整传递。
- 覆盖整票视口、字段上下文裁切和边界收敛；覆盖 Android 缺失文件安全失败和 iOS 有效 JPEG 缩略解码。

## 本轮构建基线

- 最新命令 `./gradlew spotlessCheck jvmTest testAndroidHostTest iosSimulatorArm64Test :androidApp:assembleDebug` 执行成功。
- 自动化测试共 471 项：JVM 164 项、Android Host 156 项、iOS Simulator 151 项；失败、错误和跳过均为 0。
- Android Debug APK：62,238,985 字节，SHA-256 `c06cf10fe4d784a6344fb66991daec66ac1b20298de076915040fb67cf59f1cf`。
- iOS Simulator `iosApp` Debug 应用已使用 Xcode 完整构建成功；本轮共享 iOS Simulator 测试同时完成编译、链接和执行。
- Android 真机已完成新 APK 覆盖安装、Activity 冷启动和进程存活检查；设备当时处于锁屏状态且系统禁止 ADB 输入注入，因此未把本次记录为界面交互通过。
- iPhone 15 Pro 真机已使用个人开发者账号完成 arm64 Debug 包签名、安装、启动和进程存活检查。
- 本轮未重新构建桌面分发包；这是 ADR-011 确定的移动 V1 范围收敛结果，不是桌面能力已完成。

## 未完成范围

- 多个期号或金额候选尚未映射到具体字段供用户选择；为避免混票或任选歧义值，目前不带入任何候选，而是进入对照原图的空白录入。
- OCR 置信度尚未按 Android ML Kit 和 iOS Vision 分别校准，低置信字段没有视觉标记。
- 安全 OCR 草稿仍按解析器识别出的投注行编辑，不尝试自动拆分或合并可疑行；无安全草稿时会进入空白多注录入，支持用户新增和删除整行，但不混用部分可疑 OCR 行。
- 真实开奖查询、规则计算和结果页已由 B5 接入；B4 的低置信字段、候选值和足量票样验收仍未完成。
- iOS Simulator 已验证生产格式 JPEG 的缩略解码；Android Host 环境没有真实 `BitmapFactory`，因此 Android 有效图片解码仍需真机验证。
- Android 16 与 iOS 26.5 模拟器已完成本轮真实票图、原图辅助录入、iOS 软键盘完成操作和临时图片清理回归；目标 Android/iPhone 物理真机上的完整字段切换、高亮、触控、横竖屏和动态字号验收仍未完成。

## 下一步

1. 在 Android 和 iPhone 真机使用单期双色球、大乐透基本票和大乐透追加票验证原图预览、字段定位及完整人工校正交互。
2. 结合更多真实票样判断期号与金额多候选是否能证明来自同一张票，再决定是否提供候选选择；证据不足时继续硬阻断。
3. 完成 Android/iOS OCR 置信度校准和足量真值验收，再决定支持白名单。
4. 配合 B5 在 Android/iPhone 真机复核“拍照或导入 → 校正 → 真实查询 → 结果或安全重试”的完整交互。
