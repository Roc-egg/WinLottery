# V1.3 历史走势与数学预测交付记录

文档状态：进行中

立项日期：2026-08-26

当前稳定版本：`1.2.0 (4)`

目标平台：Android、iOS

## 1. 当前目标

V1.3 优先交付 Android/iOS 共用的真实历史开奖基本走势图。当前阶段已经完成官方历史开奖会话采样、号码区域规格、出现次数、当前遗漏、最大遗漏、期号号码矩阵和共享 Compose 页面；ADR-023 已冻结首发数学策略与样本外回测方案，正在进入共享领域实现。

## 2. 冻结边界

- 大乐透支持前区 `01..35`、后区 `01..12`；双色球支持红球 `01..33`、蓝球 `01..16`。
- “当前往前”统一解释为以官网当前最新已发布期为锚点，样本范围固定为最近 `50 / 80 / 120 / 300 / 500` 期。
- 每个彩种首次进入走势图时最多加载 500 期；彩种内的号码区域和样本档位切换只使用当前控制器会话内存，不重复请求官网。
- 不使用演示开奖兜底。网络或官网结构失败时展示明确失败状态，只允许用户主动重试，不把缺失数据解释成真实走势。
- 命中格显示号码圆点，未命中格显示截至该期的样本内遗漏值；号码表头后先展示出现次数、当前遗漏和最大遗漏，再展示各期开奖行。
- 图表显示真实样本数量、起止期号、缺期状态、官方来源和本次获取时间；连线只表达相邻期命中位置，不表达因果或中奖概率。
- 走势图、状态和计算均位于共享模块，不为 Android 或 iOS 分别实现业务口径。
- 当前只允许用户操作触发的低频会话采样；不新增历史开奖 Room 表、不保存官网原始响应、不后台轮询、不长期归档。
- 数学预测不与基本走势图捆绑完成，必须先冻结算法、参数、时间前推回测和均匀随机基线。

## 3. 进度

| 进度 | 状态 | 验收证据 |
|---|---|---|
| ADR-021 与 V1.3 立项 | 已完成 | 决策记录、路线图和主页状态一致 |
| ADR-022 会话内真实开奖采样 | 已完成 | 最新期锚点、五档样本、内存边界和失败关闭已冻结 |
| 历史走势领域模型与统计 | 已完成 | JVM 共享测试通过 |
| 官方历史开奖分页与校验 | 已完成 | 大乐透每页 100 期分页、双色球单页 500 期及结构漂移测试通过 |
| 跨平台真实走势图 | 已完成 | 异步加载、失败重试、会话缓存及 Android/iOS 编译通过 |
| 双虚拟机真实走势图验收 | 已完成 | 固定双虚拟机的双端 Release、标准与最大辅助字号专项均通过 |
| 走势图统计前置优化 | 已完成 | 三行统计位于号码表头与首期开奖之间，500 期无需滚到样本末尾 |
| 历史开奖长期持久化 | 阻断 | 尚未取得长期存储、分析和再展示授权，不新增 Room 表 |
| 数学预测与样本外回测 | 进行中 | ADR-023 已冻结固定策略、50 期窗口、450 期前推回测和精确随机基线 |
| 完整 V1.3 版本关闭 | 阻断 | 数学预测与回测尚未完成；公开分发还受数据授权约束 |

## 4. 完成条件

- 共享数据测试覆盖分页、玩法标识、发布状态、期号、日期、号码范围、唯一性、顺序、重复记录、来源漂移和网络失败。
- 共享领域测试覆盖号码越界、重复号码、重复期号、乱序、跨彩种、缺期和冲突。
- Android/iOS 共用页面支持彩种、号码区域和五档真实样本切换，横向矩阵在标准与最大辅助字号下不重叠。
- 页面最后一期必须等于验收时官网最新已发布期，样本档位必须展示精确的实际期数；静态演示期号不得进入正式验收。
- 预测策略可由历史快照、算法版本和参数复现，并按时间前推与均匀随机基线比较。
- 固定双虚拟机、共享测试和双端 Release 构建通过后，才能评估当前走势图阶段关闭；完整 V1.3 仍需数学预测阶段结论。

## 5. 已完成验证

### 2026-08-26：官方历史开奖数据层

- 官网实测确认中国体彩网大乐透接口每页最多返回 100 期，取得 500 期需要 5 页；中国福彩网双色球接口可单页返回 500 期。
- `./gradlew :shared:data:jvmTest`：通过。覆盖大乐透跨页精确截取、双色球 500 期单页、请求上限、网络失败和分页响应严格校验。
- 原始响应只在单次调用内解析；对外只返回按开奖时间正序排列的 `HistoricalDraw`，当前会话结束后不保存。

### 2026-08-26：共享状态与跨平台编译

- `./gradlew :shared:domain:jvmTest :shared:jvmTest`：通过。
- `./gradlew :shared:compileAndroidMain :shared:compileKotlinIosSimulatorArm64`：通过。
- 默认加载真实 50 期；切换 `80 / 120 / 300 / 500` 期和号码区域只重算内存快照，切换到另一个彩种时才加载该彩种的 500 期。
- 加载中、失败、主动重试和迟到响应丢弃均由共享控制器管理；Android 与 iOS 使用同一个 `OfficialDrawRepository` 实例提供单期核对和历史走势数据。

### 2026-08-26：双虚拟机真实数据验收

- `./tools/mobile/run-current-vm-v1-3-checks.sh`：通过。Android Release 实际执行 `173` 个任务，R8、Spotless、APK/AAB 签名记录、AAB 压缩完整性、安装、冷启动和非 Debug 进程检查均通过；APK 为 `168259345` 字节，AAB 为 `117791542` 字节。
- iOS Simulator Release 显示 `BUILD SUCCEEDED`，安装、启动和非 Debug 进程检查通过；`.app` 为 `109180 KiB`，属于 Simulator 本地签名产物，不是发布 IPA。
- 锁定 `Medium_Phone_API_36.1 / emulator-5554` 与 `iPhone 17 Pro / iOS 26.5`，Android 标准字号、iOS 标准字号、Android 最大字号和 iOS 最大字号四组完整交互全部通过。
- 验收脚本在每个字号模式启动时动态读取官网最新已发布期，不硬编码期号。本轮两次读取均为大乐透 `26096`、双色球 `2026098`；页面五档最后一期全部与官网一致。
- 大乐透 50 期实测范围为 `26047..26096`，500 期实测范围为 `23049..26096`；`50 / 80 / 120 / 300 / 500` 五档切换、双色球会话切换和一级页面往返保留均通过。
- 双端竖屏矩阵可横向浏览且固定期号列不位移；双端横屏完整展示大乐透前区 `01..35`；标准与最大辅助字号下筛选、来源、矩阵、三行遗漏统计和责任提示均无重叠。
- 本轮新生成 28 张正式截图，时间为 `15:04..15:07`，证据位于 `build/reports/v1-3-trends/`。
- Android 验收在启动 Appium 前会等待其他 instrumentation 自然释放 `UiAutomation`，不会强制终止同一模拟器上的既有任务；瞬态未连接只做三次有限重试，其他错误仍立即失败。

### 2026-08-26：走势图统计前置优化

- Android/iOS 共用页面已将出现次数、当前遗漏、最大遗漏移到号码表头之后、各期开奖行之前；竖屏与横屏保持同一阅读顺序，统计值继续与号码列共享横向滚动位置。
- Appium 专项新增统计行实际坐标断言，要求 `出现次数 → 当前遗漏 → 最大遗漏 → 首期开奖` 完整可见、顺序正确且互不重叠；500 期和双色球 50 期均覆盖，不再通过滚动到样本末尾寻找统计。
- `./gradlew :shared:compileAndroidMain :shared:compileKotlinIosSimulatorArm64`：通过。
- `./tools/mobile/run-current-vm-v1-3-checks.sh`：通过。Android Release `173` 个任务、R8、Spotless、lint、APK/AAB 签名与压缩校验、安装和冷启动均通过；APK 为 `168259345` 字节，AAB 为 `117791499` 字节。
- iOS Simulator Release 为 `BUILD SUCCEEDED`，安装、启动和非 Debug 进程检查通过；`.app` 为 `109180 KiB`。本次仍只验证 Simulator 本地签名产物，不涉及真机或发布 IPA。
- Android 标准字号、iOS 标准字号、Android 最大辅助字号、iOS 最大辅助字号四组完整交互均通过；验收时官网最新期为大乐透 `26096`、双色球 `2026098`，大乐透 500 期范围为 `23049..26096`。
- 本次刷新 36 张正式截图，时间为 `15:41..15:44`。人工复核确认双端竖屏 500 期与横屏截图中的表头、三行统计、首期开奖顺序正确，最大辅助字号无重叠；证据位于 `build/reports/v1-3-trends/`。

### 2026-08-26：数学策略与回测协议冻结

- ADR-023 已采用。首发只实现 `frequency-omission-ranking-v1`：最近 50 期内按出现次数升序、当前遗漏降序、号码升序确定性选出一注候选，不开放参数调节或事后策略切换。
- 最近 500 期按时间前推形成 450 个样本外目标期，每个目标期只能看到此前 50 期；生产候选和回测必须调用同一策略实现。
- 主号码、次号码分别展示策略平均命中数、均匀无放回选号的精确理论期望 `k² / N` 和差值。当前阶段固定说明尚未证明稳定优于随机，不使用中奖个例、奖金或稀疏奖级作宣传。
- 历史开奖仍只存在当前控制器会话内；本阶段不新增 Room 表、网络请求、后台任务、收藏或分享。
