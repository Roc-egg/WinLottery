# V1.3 历史走势与数学预测交付记录

文档状态：进行中
立项日期：2026-08-26
当前稳定版本：`1.2.0 (4)`
目标平台：Android、iOS

## 1. 当前目标

V1.3 优先交付 Android/iOS 共用的基本走势图。首批实现历史开奖领域模型、号码区域规格、出现次数、当前遗漏、最大遗漏和期号号码矩阵，再接入共享 Compose 一级页面。

## 2. 冻结边界

- 大乐透支持前区 `01..35`、后区 `01..12`；双色球支持红球 `01..33`、蓝球 `01..16`。
- 样本范围固定为最近 `10 / 20 / 30 / 50` 期。
- 命中格显示号码圆点，未命中格显示截至该期的样本内遗漏值；底部展示出现次数、当前遗漏和最大遗漏。
- 图表显示样本数量、起止期号、缺期和冲突状态；连线只表达相邻期命中位置，不表达因果或中奖概率。
- 走势图、状态和计算均位于共享模块，不为 Android 或 iOS 分别实现业务口径。
- 首阶段只使用测试夹具、明确标记的演示快照或当前会话内合法取得的数据。未获得授权前不新增历史开奖 Room 表、不批量采集、不后台轮询。
- 数学预测不与首批走势图捆绑实现，必须先冻结算法、参数、时间前推回测和均匀随机基线。

## 3. 进度

| 进度 | 状态 | 验收证据 |
|---|---|---|
| ADR-021 与 V1.3 立项 | 已完成 | 决策记录、路线图和主页状态一致 |
| 历史走势领域模型与统计 | 已完成 | JVM 与 iOS Simulator ARM64 共享测试通过 |
| 跨平台基本走势图 | 已完成 | 竖屏紧凑浏览、横屏 `01..35` 完整矩阵与双虚拟机两档字号验收通过 |
| 获授权历史数据接入与 Room 迁移 | 阻断 | 尚未取得长期存储、分析和再展示授权 |
| 数学预测与样本外回测 | 未开始 | 待独立 ADR 冻结策略与回测协议 |
| 双虚拟机走势图验收 | 已完成 | 只使用锁定 Android Emulator 和 iOS Simulator |
| 完整 V1.3 版本关闭 | 阻断 | 历史数据授权、持久化和数学回测尚未完成 |

## 4. 完成条件

- 共享领域测试覆盖号码越界、重复号码、重复期号、乱序、跨彩种、缺期和冲突。
- Android/iOS 共用页面支持彩种、号码区域和样本范围切换，横向矩阵在标准与最大辅助字号下不重叠。
- 受控演示快照始终有清晰标识，不与实时官方开奖数据混淆。
- 获授权历史数据能通过显式 Room 迁移长期保存，修订与来源冲突不会进入图表或预测输入。
- 预测策略可由历史快照、算法版本和参数复现，并按时间前推与均匀随机基线比较。
- 固定双虚拟机、共享测试和双端 Release 构建通过后，才能评估完整 V1.3 关闭。

## 5. 已完成验证

### 2026-08-26：共享领域计算

- `./gradlew :shared:domain:jvmTest`：通过。
- `./gradlew :shared:domain:iosSimulatorArm64Test`：通过。
- 覆盖大乐透与双色球四个号码区域、乱序输入、最近样本截取、样本内遗漏、出现次数、当前遗漏、最大遗漏、同年度缺期、跨年度未知边界、空数据、非法期号、重复期号、内容冲突、跨彩种和号码非法。

### 2026-08-26：跨平台基本走势图

- `./gradlew spotlessCheck :shared:jvmTest :shared:iosSimulatorArm64Test`：通过，共享页面和会话状态在 JVM 与 iOS Simulator ARM64 目标完成编译与测试。
- Android Release R8 构建实际执行 `173` 个任务并通过，配置签名 APK、AAB 签名记录、压缩完整性、覆盖安装、冷启动和非 Debug 进程检查通过。
- iOS Simulator Release Xcode 构建显示 `BUILD SUCCEEDED`，安装、启动和非 Debug 进程检查通过；该产物仅为 Simulator 本地签名 `.app`，不是发布 IPA。
- 锁定 `Medium_Phone_API_36.1 / emulator-5554` 和 `iPhone 17 Pro / iOS 26.5`，Android/iOS 的标准字号与最大辅助字号完整交互均通过。
- 竖屏压缩筛选区和状态摘要，号码矩阵进入首屏并继续支持横向浏览；横屏改用紧凑图标导航、单行筛选、固定分区与号码表头，开奖号和统计区域独立纵向滚动。
- 大乐透前区按 `01..12 / 13..24 / 25..35` 分为一区、二区和三区，双色球红球按 `01..11 / 12..22 / 23..33` 分区；横屏最宽大乐透前区无需横向滚动即可完整展示 `01..35`。
- 高密度表格内部文字使用稳定物理尺寸，筛选、状态和导航仍跟随系统辅助字号；Android 方向切换不会重建共享控制器或丢失当前走势图状态。
- 双端验收覆盖竖横屏切换、四项一级导航不重叠、大乐透前区默认 30 期、最右 `35` 号表头完整可见、双色球蓝球 50 期、彩种/区域/样本切换、首末期可达、竖屏矩阵真实横向滑动、固定期号列不位移、三行遗漏统计、责任提示完整可见和一级页面往返后的会话保留。
- 截图证据位于 `build/reports/v1-3-trends/`；演示状态始终显示“受控演示快照，非实时官方开奖，不参与预测”。
